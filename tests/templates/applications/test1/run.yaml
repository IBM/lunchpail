{{ if or (not .Values.app) (eq .Values.app "tests") }}
apiVersion: codeflare.dev/v1alpha1
kind: Run
metadata:
  name: test1
  namespace: {{ .Values.namespace }}
spec:
  application:
    name: test1
{{ end }}
