datashim:
  apiVersion: com.ie.ibm.hpsys/v1alpha1

datasets:
  s3:
    secret:
      name: cfp-test-dataset-s3

github_ibm_com:
  secret:
    name: github-ibm-com-jaas
    user: xxx # replaced by HELM_SECRETS in deploy-tests.sh
    pat: yyy # replaced by HELM_SECRETS in deploy-tests.sh

# these values may be overriden by hack/up.sh
image:
  registry: ghcr.io
  repo: lunchpail
  version: 0.0.1


# for nfs tests
nfs:
  enabled: false
  clusterIP: 10.96.93.144
