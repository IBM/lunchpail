{{ if or (not .Values.app) (eq .Values.app "tests") (eq .Values.app "test0") }}
apiVersion: codeflare.dev/v1alpha1
kind: Run
metadata:
  name: test0
  namespace: {{ .Values.namespace.user }}
spec:
  application:
    name: test0-is-not-exist # expect Failed
{{ end }}
