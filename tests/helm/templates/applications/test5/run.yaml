{{ if and (.Values.supportsGpu) (or (not .Values.app) (eq .Values.app "tests") (eq .Values.app "test5")) }}
apiVersion: codeflare.dev/v1alpha1
kind: Run
metadata:
  name: test5
  namespace: {{ .Values.namespace.user }}
spec:
  application:
    name: test5
{{ end }}
