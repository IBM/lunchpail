{{ if or (not .Values.app) (eq .Values.app "tests") (eq .Values.app "test7") }}
apiVersion: v1
kind: Secret
metadata:
  name: test7data-cfsecret # avoid dashes for now: https://github.com/datashim-io/datashim/issues/285
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  accessKeyID: {{ .Values.global.s3AccessKey | b64enc }}
  secretAccessKey: {{ .Values.global.s3SecretKey | b64enc }}
{{- end }}
