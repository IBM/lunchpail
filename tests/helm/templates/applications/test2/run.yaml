{{ if or (not .Values.app) (eq .Values.app "tests") (eq .Values.app "test2") }}
apiVersion: codeflare.dev/v1alpha1
kind: Run
metadata:
  name: test2
  namespace: {{ .Values.namespace.user }}
spec:
  application:
    name: test2
  workers: 0
{{ end }}
