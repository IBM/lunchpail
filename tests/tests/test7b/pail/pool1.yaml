{{ if or (not .Values.app) (eq .Values.app "tests") (eq .Values.app "test7b") }}
apiVersion: codeflare.dev/v1alpha1
kind: WorkerPool
metadata:
  name: test7b-pool1
  namespace: {{ .Values.namespace.user }}
spec:
  run:
    name: test7b
  workers:
    count: 2
    size: xxs
  env:
    xxx: "3333333333" # only override the xxx key from 111... to 333...
{{- end }}
