{{ if or (not .Values.app) (eq .Values.app "tests") (eq .Values.app "test0b") }}
apiVersion: lunchpail.io/v1alpha1
kind: Run
metadata:
  name: test0b
  namespace: {{ .Values.namespace.user }}
spec:
  application:
    name: test0b
{{ end }}
