{{ if or (not .Values.app) (eq .Values.app "tests") (eq .Values.app "test7") }}
apiVersion: lunchpail.io/v1alpha1
kind: WorkerPool
metadata:
  name: test7-pool1
spec:
  workers:
    count: 2
    size: auto
{{- end }}
