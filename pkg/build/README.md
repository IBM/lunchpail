This package deals with preserving and restoring the attributes of a
build, such as:

- Lunchpail itself, which enables rebuilding with new values from an existing build
- application source
- key-value assignments to be sent to the run
