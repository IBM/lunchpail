apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workdir
  namespace: {{ .Values.controllers.namespace }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: {{ .Values.workdir.size }}
  volumeName: workdir
