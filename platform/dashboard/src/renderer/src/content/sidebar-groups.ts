import type { ContentProviderSidebarSpec } from "./ContentProvider"

/** Group for generic resources */
export const resourcesGroup = "Resources"

/** Sidebar Group model for generic resources */
export const resourcesSidebar: ContentProviderSidebarSpec = {
  group: resourcesGroup,
}
