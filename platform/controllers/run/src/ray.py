import logging

def create_run_ray(v1Api, application, application_namespace, name, spec, command_line_options, run_size_config, patch):
    logging.info(f"Handling Ray Run: {application['metadata']['name']}")
    pass

