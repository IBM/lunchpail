apiVersion: apps/v1
kind: Deployment
metadata:
  name: codeflare-application-controller
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: application-controller
    app.kubernetes.io/part-of: codeflare.dev
    app.kubernetes.io/component: controller
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: application-controller
      app.kubernetes.io/part-of: codeflare.dev
      app.kubernetes.io/component: controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: application-controller
        app.kubernetes.io/part-of: codeflare.dev
        app.kubernetes.io/component: controller
    spec:
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      serviceAccountName: codeflare-application-controller-account
      volumes:
        {{- include "volume.workdir.nfs" . | indent 8 }}
      containers:
      - name: controller

        {{ if .Values.global.lite }}
        image: {{ .Values.controllers.application.image.lite }}
        {{ else }}
        image: {{ .Values.controllers.application.image.full }}
        {{ end }}

        env:
          - name: WORKDIR
            value: /workdir
          - name: WORKDIR_PVC
            value: workdir
        volumeMounts:
        - name: workdir
          mountPath: /workdir
